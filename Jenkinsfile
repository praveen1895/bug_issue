pipeline{
    agent any
    stages{
        stage(build)
        {
            steps{
                echo "stage build"
            }
        }
        stage(test)
        {
            steps{
                echo "stage testing"
            }
        }
        stage(deploy)
        {
            echo "stage deploy"
        }

    }
}
